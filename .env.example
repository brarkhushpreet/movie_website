# TMDB Read Access Token (v4). Keep this server-side.
TMDB_API_KEY=your_tmdb_read_access_token

# PostgreSQL connection used for accounts, sessions, profiles, My List, and preferences.
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
